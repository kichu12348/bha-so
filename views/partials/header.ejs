<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>College Club Management</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-gray-800 text-white p-4 shadow-lg">
    <div class="max-w-7xl mx-auto flex justify-between items-center">
      <div class="flex space-x-6">
        <a href="/clubs" class="hover:text-gray-300 font-semibold">All Clubs</a>
        
        <% if (user && user.role === 'admin') { %>
          <a href="/clubs/new" class="hover:text-gray-300">Create Club</a>
        <% } %>
      </div>
      
      <div class="flex items-center space-x-4">
        <% if (!user) { %>
          <a href="/login" class="hover:text-gray-300">Login</a>
          <a href="/register" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded">Register</a>
        <% } else { %>
          <span class="text-gray-300">Logged in as <strong><%= user.name %></strong> (<%= user.role %>)</span>
          <form action="/logout" method="POST" class="inline">
            <button type="submit" class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded">Logout</button>
          </form>
        <% } %>
      </div>
    </div>
  </nav>
  
  <main class="max-w-7xl mx-auto p-6">
